---
name: CI Minimal Echo

on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Debug Info
        run: |
          echo "🔄 CI minimal fonctionnel !"
          echo "Branch: $GITHUB_REF"
          echo "Job ID: $GITHUB_JOB"
