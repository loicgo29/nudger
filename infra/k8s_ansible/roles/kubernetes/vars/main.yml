---
# Docker (optional if you also want Docker runtime)
docker_dependencies:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg
  - lsb-release
docker_gpg_key: "https://download.docker.com/linux/ubuntu/gpg"
docker_repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release
  }} stable"
docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
