---
- name: Bootstrap Flux (secret + GitRepository + Kustomizations)
  hosts: k8s_masters
  become: true
  gather_facts: yes

  roles:
    - role: node_longhorn_prep         # ton rôle existant (packages, multipathd off, dm_crypt, etc.)
    - role: disable_ingress_dnat       # nettoie tout hack DNAT PREROUTING 80/443
    - role: egress_nat_nftables        # pose le SNAT pods -> Internet (nftables)
    - role: ingress_dnat
