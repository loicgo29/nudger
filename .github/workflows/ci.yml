name: File Check
on: push
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          echo "📂 Contenu du dépôt :"
          ls -l
          test -f Vagrantfile && echo "✓ Vagrantfile présent" || echo "⚠️ Vagrantfile manquant"
